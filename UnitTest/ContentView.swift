import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack{
            Text("SwiftUI")
            HStack{
                Text("H")
                Text("Stack")
            }
        }
    }
}
